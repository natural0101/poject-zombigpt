Step 30 — the Windows test suite, run by run
============================================

Each line is the pytest summary of the `windows package` workflow's test stage
on windows-latest / CPython 3.12.10. A Linux result says nothing about any of
this, which is why the numbers come from the workflow and nowhere else.

  873037c0  run 31156087690   24 failed, 3633 passed, 22 skipped   (branch point)
  df2db383  run 31160929042   13 failed, 3679 passed, 22 skipped
  ce707119  run 31162211524    1 failed, 3700 passed, 22 skipped
  2c4fd3c8  run 31162649795    0 failed, 3714 passed, 22 skipped

The last failure, at ce707119, was
`test_verify_flags_a_member_that_reached_the_archive_unredacted`:

  AssertionError: assert 'user_dir' in ('home_dir', 'windows_user_profile',
                                        'absolute_path', 'literal:account_name')

Not a test artefact. The redactor enumerated whole spellings of a literal path
— all-`/`, all-`\`, all-doubled, all-percent-encoded — so a path that *mixed*
them (`C:\Users\Иван/Zomboid`, which is what `f"{path}/name"` produces) matched
none of them and fell through to the shorter `home_dir` literal. The path was
still struck out, so this was not a leak; it came out under `<USER_HOME>`
instead of `<ZOMBOID>` and was reported under the wrong label, which means the
same file produced a different line on Windows than on Linux. That is the one
guarantee a placeholder exists to provide.

Fixed at 2c4fd3c8 by matching each separator position independently rather than
enumerating mixtures, which are exponential in the number of separators.
Regression tests: tests/contract/test_windows_path_shapes.py, six of which fail
against the previous redactor.

The workflow itself is *not* green at 2c4fd3c8. It fails one stage later, in
the PyInstaller build of pz-agent-mcp.exe:

  RuntimeError: Child process call to _collect_submodules() failed with:
    File ".../mcp/cli/cli.py", line 15, in <module>
      import typer
  ModuleNotFoundError: No module named 'typer'
  ...
  SystemExit: 1

`pz-agent.exe` built and the tests passed; this is stage 10's territory
(step 92) surfacing early because the workflow builds both. Addressed at
15296e50 with packaging/windows/specutil.py. Step 30 stays IN_PROGRESS until a
run is green end to end — the test suite being green is a necessary part of
that and not the whole of it.

Closed
------

  a7c16ba2  run 31163394899   0 failed, 3714 passed, 22 skipped

and, for the first time on this branch, every later stage of the workflow too:

  Tests                                            success
  Build pz-agent.exe                               success
  Build pz-agent-mcp.exe                           success
  Both executables answer                          success
  Assemble the archive                             success
  Release gate                                     success
  Upload the release candidate                     success

No step skipped. The `pz-agent-mcp.exe` build was fixed at 15296e50 by
packaging/windows/specutil.py, which reads the SDK's package directory instead
of importing each module to discover it — `mcp.cli` calls `sys.exit` at import
time without its optional `typer` extra, and a `SystemExit` is not an
`Exception`, so PyInstaller's `on_error` could not skip it.

Nothing was loosened to get here: no assertion removed, no Windows failure
turned into a skip, and the workflow runs every check it ran at the branch
point. Steps 91-95 are not claimed on this run — they come after the RPC, MCP
and voice stages in the plan, and will need their own green run once those
land.

--- 2026-08-07: run 31214158408 on main at 276b9d90 ---

https://github.com/natural0101/poject-zombigpt/actions/runs/31214158408
windows-latest, conclusion: success, every step green.

  Tests                                            success
  Build pz-agent.exe                               success
  Build pz-agent-mcp.exe                           success
  Both executables answer                          success
  Assemble the archive                             success
  Release gate                                     success
  Upload the release candidate                     success

Observed for E11-M02-T004: the "Both executables answer" step ran
pz-agent-mcp.exe and the reports artifact (pz-agent-windows-reports,
sha256 6f5beaea...) contains pz-agent-mcp-catalogue.json written by the
executable on that runner.

Observed for E11-M03-T001: the "Assemble the archive" step produced the
release zip and it was uploaded as pz-agent-windows-rc
(sha256 3eb5ca63c4d113f3a3a48914a7cd206f77439a24a80f891fcea60b205733bbe2).

--- The failure trend, per run, so the trajectory is auditable ---

Counts are test failures in the windows-latest Tests step of the run named;
"gate" marks a run red on a checking step rather than on tests. Only runs
whose counts were read from their logs at the time are listed with a number —
a run nobody read is listed as its conclusion, not as a guess.

| run | commit | failures |
| --- | --- | --- |
| 31156087690 | 873037c0 | 24 (the baseline split into six root causes, D-002) |
| (branch runs) | — | 24 -> 13 -> 1 -> 0 at a7c16ba2 — first green suite; then new work regressed it |
| 31187551234 | df03dba7 | 0 (green; the merge of the branch into main) |
| 31211673638 | f0fb8fb8 | 2 (both TeamON bridge death-detection cases, fixed at 370dc4b) |
| 31214158408 | 276b9d90 | 0 (green; RC built and certified from this run) |
| 31218129893 | 3c70712f | gate (the unsatisfiable GREEN rule refusing its own recording; fixed at 743115c) |
| 31223693322 | 40d528ca | 0 (green; the fixed rule's first live validation — the STATUS recorded at this commit survived its own CI) |

Every red run between 31127833879 and 31155356132 belongs to the descent from
24: each fixed a subset of the six causes and the next run measured the rest.

| 31231766866 | f8624bb6 | hang (the Tests step ran 30+ minutes and never finished: the two new R-002
boundary tests raised through asyncio.run over a monkeypatched coroutine, which
Linux tolerated and windows-latest did not; cancelled by the next push) |

The lesson became structure twice over: the boundary tests are rewritten
loop-free — what they test is one except clause, which needs no event loop —
and the suite itself gained the bound it preached (pytest-timeout, 300 s per
test, thread method), so the next hang is a named failure instead of a runner
held to the platform's six-hour ceiling.

| 31235583344 | e75754e1 | 0 (green; the loop-free boundary tests and the 300 s per-test bound both in
effect — the hang did not recur, and if it ever does, the bound names the test) |

| 31246415811 | a82b8b53 | 6 (three causes, all from the goal-channel/transport iteration, none reproducible
on Linux: (a) the transport rework's hand-dialled socket referenced socket.AF_UNIX
unconditionally — a FAMILY_UNIX descriptor on Windows crashed with AttributeError
instead of reporting the sidecar unreachable; (b) the two new server-survival tests
left the server's idle budget at 60s, and a Windows named-pipe read that began
before the peer vanished has no hard deadline, so the abandoned handshake held the
single serving thread past the follow-up client's patience; (c) the new
account-name evidence test buried a synthetic profile segment under pytest's temp,
which the floor redactor targets profile *leads* not mid-path Users/ segments —
correct behaviour, over-strict test). Fixes: an AF_UNIX-absence guard in _dial that
raises the transport's own unreachable error; a short injected idle budget on the
two server-survival tests; the evidence test rewritten to assert the floor-redacted
spelling and a genuinely profile-rooted redaction rather than a temp-buried string.
Diagnosed from the log on Linux, per the standing rule; the next run is the judge.) |

| 31247921064 | 8929434c | 3+1e (the judge's verdict on the row above: fixes (a) and half of (c)
held — the two mcp-entry AttributeErrors and the manifest account-name test are gone.
What remained: (b) was misdiagnosed as an idle-budget problem when it was a guard-shape
problem — with the short budget the vanished peer now DOES end the server's wait on
Windows, but as BrokenPipeError raised from connection.poll itself, which sat outside
the recv-drop guard in _exchange; the exception unwound serve_forever (the run's one
ERROR: unhandled thread exception) and the follow-up client timed out against a link
nobody served. On Unix the same hang-up answers the poll and raises EOFError from the
recv, inside the guard — one fact, two platform spellings, one of them unguarded. And
(c) recurred in the two sibling tests the first rewrite missed: TestLayout's
outside-tree test and TestCollection's account-name test planted the same
Users/<name> segment under pytest's temp, which on Windows lives under the runner's
profile, so the planted segment sat mid-path past the stripped lead. Fixes: the idle
poll moved inside _exchange's drop guard, with a seam-pinning test (a real Unix
socket cannot raise from poll, so the Windows spelling is driven directly); both
sibling tests rewritten to pin the report's spelling to the floor redactor's exactly,
plus direct profile-rooted probes for the account-name promise. The next run is the
judge — now on rescue/today-finalization, the swarm integration branch.) |

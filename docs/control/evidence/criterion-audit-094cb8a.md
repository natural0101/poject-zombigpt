Criterion-coverage audit of the 75 heaviest PASS claims (weight >= 8), run at 094cb8a.

Twelve read-only auditors, one question per task: does the named regression test
OBSERVE the stated pass_criterion, such that the criterion becoming false fails
the test? 53 confirmed with the observing assertion named. 22 refused, each with
the precise gap. The two product-level findings became R-008 and R-009; the rest
are missing assertions over behaviour that exists.

The 22, each with its gap:

### E11-M04-T007
Nothing asserts the wiring the criterion actually names: no test checks that the real plan's archive tasks depend (directly or transitively) on the per-member verification task, so deleting E11-M03-T003 from the depends_on chain — the only mechanism making the criterion true — fails no test. Nor does anything assert the verification 'has run': the gate only reads recorded statuses, and no test observes that a ZIP whose contents were never checked is not counted. test_check_release.py covers per-member hashing itself (E11-M03-T003's criterion), not this task's claim about the plan gate.

### E12-M02-T004
Three of the four waits on another process are asserted nowhere. (1) The server's idle wait connection.poll(IDLE_SECONDS) (transport.py:228): changing it to a blocking poll(None) fails no test — no test opens an authenticated connection, sends nothing, and observes the drop. (2) The client's connection.recv_bytes after a successful poll (transport.py:364) has no deadline in the code at all — a server that sends the length header then trickles the payload passes poll and blocks recv_bytes indefinitely, so the criterion is arguably false in the shipped code and certainly unobserved. (3) The Client() connect/auth handshake (transport.py:347) is unbounded — a peer that accepts but never completes the challenge hangs the client forever, untested. The named test therefore does not fail for most ways 'every poll and read has a deadline' can become false.

### E12-M01-T001
Nothing in the named test (or any other test found: only test_mcp_configs.py checks secret shapes, and only inside configs/mcp) asserts either (a) that check_secrets() fires on a planted secret — the rule's continued existence — or (b) that check_secrets() over the repository's tracked files returns no findings — the criterion itself. 'The secret scanner finds nothing tracked' is observed solely by executing scripts/check_forbidden.py, not by the regression test.

### E12-M01-T002
Nothing asserts over an actual log record or log file: if any core-side code (token issuance, the RPC transport handling authkey, the sidecar lifecycle) logged the token — e.g. logging.info('issued %s', token.hex()) into the diagnostics log — no test would fail, because no test ever captures log records or scans the sidecar's log file for the token. The criterion 'no log record contains the token' is inferred from exception-message and stream hygiene, not observed. (The redactor tests in test_diagnostics_redaction.py exercise generic 'token=' patterns, never the real RPC token bytes/hex.)

### E12-M01-T003
No test anywhere observes 'no bundle member contains the RPC token'. The real bundle is assembled by pz_agent_cli.support.build_support_bundle (versions/doctor/capabilities/config/logs/traces/ipc-journal members); no test issues a real token (pz_agent_core.rpc.token.issue_token) into a state directory, builds that bundle, and scans every member's bytes for the token value or its hex. The named test exercises only the generic BundleBuilder redactor on a hand-written field named 'token'; tests/unit/test_diagnostics_redaction.py checks the redactor component on token-shaped strings but never a built bundle; the CLI bundle tests in test_cli_app.py check save files and game source, never the token; and verify_bundle's 'forbidden' map in support.py carries directory/account names only, never the token. If a future member (e.g. a log tail or doctor.json) started carrying the token, no test would fail.

### E12-M03-T002
In the named file tests/unit/test_actions_engine.py, no assertion covers a game dying after the command is dispatched: nothing asserts an in-flight action (sink.sent non-empty) terminates with GAME_DISCONNECTED and a non-succeeded status. That mid-action ending is asserted only in tests/unit/test_session_recovery.py::test_the_engine_does_not_keep_trying_against_a_game_that_is_gone (engine layer) and ::test_work_in_flight_when_the_game_went_is_closed_lost_and_never_succeeded (session layer).

### E12-M03-T003
No test anywhere drops an established, authenticated connection mid-exchange. The recovery code the criterion names is untested on both ends: RpcClient.call's except (OSError, EOFError) -> RpcUnavailable('the sidecar closed the connection') around send_bytes/recv_bytes (transport.py:365-368) is reached by no test — deleting it fails nothing; likewise the server's _exchange recv-drop guard (transport.py:232-233) and _reply's suppress(OSError, EOFError) (transport.py:262-263), so a sidecar that died when a client vanished after auth — the literal 'link drops mid-call, sidecar keeps serving' claim — would still pass the whole file. A repo-wide grep finds no test exercising the 'closed the connection' branch.

### E08-M03-T010
Nowhere does a real client submit, poll, and cancel a goal against a running core. The pz_goal_* tools are exercised only in-process against doubles (tests/unit/test_mcp_router.py, tests/unit/test_mcp_catalog_goals.py — no subprocess, no running core). The closest cross-process test, tests/contract/test_remote_core_round_trip.py::test_every_declared_method_survives_a_real_call, sends goal.submit/goal.status/goal.cancel over the RPC socket but is not an MCP client, cancels a nonexistent goal id ('no-such-goal'), and deliberately tolerates CoreRefused, so it does not observe a submit→poll→cancel lifecycle. tests/contract/test_goal_reaches_the_planner.py is entirely in-process core code with no client. If the MCP goal tools broke end to end, the named regression test would still pass.

### E12-M01-T005
The criterion says 'for every shape', and three of the eight SECRET_RULES in packages/pz_agent_core/src/pz_agent_core/diagnostics/redaction.py (lines 271-285) have no absence assertion anywhere in the suite: openai_key (bare 'sk-' + 16 alnum — the tested 'sk-live-...' strings are matched by credential_assignment via their 'api_key=' label, not by this rule), github_token ('ghp_...'), and aws_key_id ('AKIA...'). The only 'ghp_' reference in tests is tests/unit/test_mcp_configs.py scanning config snippets, not the redactor. If any of those three rules regressed — pattern broken, or replacement changed to annotate rather than remove — no test in the named file (or anywhere) would fail, so 'absent for every shape' is only partially observed.

### E12-M03-T004
The criterion 'the sidecar reports it rather than losing the session silently' is asserted nowhere. A repo-wide search for unwritable/PermissionError/write-failure tests finds only different subjects: tests/unit/test_safety_stop.py::test_the_state_directory_really_cannot_be_written wrecks the IPC exchange dir but only proves the panic latch stays READABLE; tests/unit/test_cli_app_build.py::test_a_record_that_cannot_be_written_is_noted_and_not_raised covers the capability record file only; tests/contract/test_sidecar_writes_its_log.py covers an unwritable LOG directory; tests/contract/test_voice_wiring.py covers the voice companion's mod layout. None makes the supervisor's session/pid/control writes fail and asserts the sidecar reports the condition, so the criterion could become false with the named regression test still green.

### E12-M01-T006
No test anywhere verifies a bundle whose member carries a credential and asserts the verification findings are non-empty. The criterion's composed claim — verify_bundle over an archive containing a token/key produces non-empty findings — is asserted in no test; it is covered only as two disjoint halves (credential->Redactor.findings in test_diagnostics_redaction.py; rule-findings->VerifiedEntry.findings plumbing in test_diagnostics_bundle.py, exercised with a path leak).

### E12-M03-T005
'The session refuses to arm' is asserted nowhere in the repository. test_ipc_journal.py has no arming assertion; test_session_arming.py never touches a journal; test_safety_stop.py's 'truncated_journal' wreck (_wreck_journal, test_the_truncated_journal_really_loses_its_last_record) only proves the panic latch still reads and the tear is observable, not an arming refusal; test_session_recovery.py's journal test (test_a_restarted_sidecar_does_not_re_execute_the_previous_runs_commands) is about not replaying commands, not refusing to arm. A build in which a session arms cleanly over a truncated journal passes the named test and every neighbouring suite.

### E07-M03-T005
Neither half of the criterion is asserted anywhere for the mid-session case: (a) no test asserts that a tool call made over an already-established MCP connection, after the sidecar has gone away, returns an error payload reporting the link is down (e.g. reason_code STALE_SESSION) rather than the connection dying; (b) no test asserts the pz-agent-mcp process stays alive / exits cleanly (does not crash) after such a failure — the pytest.raises(Exception) in the closest test would pass equally if the child segfaulted. The nearest real observation, tests/unit/test_envelope_failures.py::TestNothingLeaksIntoTheMessage::test_a_missing_sidecar_reaches_a_client_as_a_stale_session_with_a_remedy, maps SidecarUnavailable to a STALE_SESSION payload in-process, for a sidecar that never started — not one that went away mid-session, and with no subprocess whose survival could be observed.

### E11-M02-T005
Nothing asserts the packaged executable completes an MCP initialize over the RPC link. The only CI exercise of pz-agent-mcp.exe is .github/workflows/windows.yml's 'Both executables answer' step, which runs 'pz-agent-mcp.exe --describe' — and the suite's own test_describe_succeeding_does_not_imply_a_working_transport proves --describe answers identically with no sidecar, no socket, and no SDK. The evidence file itself records the frozen build failing (typer hidden-import) while the test suite was fully green — direct proof the named test cannot observe packaged-executable breakage. A regression in the PyInstaller bundle (missing hidden import, missing data file) that made the exe unable to initialize against a sidecar would fail no test.

### E11-M03-T003
No test performs a same-size content edit of an archive member — the only violation that hashing (as opposed to the recorded size) can catch — so deleting the '_sha256_bytes(...) != recorded' comparison from _recorded_digests leaves the entire suite green; the hash-mismatch message 'content does not match the recorded digest' is asserted nowhere. Nor does any test add a member to the archive that the index never recorded: the gate iterates manifest['files'] rather than the archive's namelist, so an unrecorded member is never hashed and no test observes that either. (tests/unit/test_check_release.py::test_an_edited_result_is_caught_by_its_digest does observe real re-hashing, but of evidence artefacts via _verify_on_disk — a different check from the archive-member verification this task names.)

### E12-M01-T008
The evidence-manifest half of the criterion is asserted nowhere. The livetest evidence writer (pz_agent_cli/livetest/evidence.py) never uses paths.py: EvidenceLayout.relative does its own relative_to, and for a path outside the tree it records path.as_posix() — the absolute, account-name-carrying spelling — a behavior tests/unit/test_livetest_evidence.py::test_a_path_outside_the_tree_is_shown_rather_than_hidden pins as correct, and that value flows into manifest artefact entries (digest_entry, evidence.py:516). No test sweeps a written evidence manifest for the account name, and TestManifestEntries never asserts the shape of entry.path; a writer that recorded str(path) instead of layout.relative(path) would fail nothing. So 'no manifest or evidence path carries it' can become false with the named test — and every other test — still green.

### E07-M03-T007
Nothing executable in the repo observes the Windows run's outcome: if the E2E suite regressed on Windows, the named test would still pass wherever the plan's gate runs. The only artifact is a hand-copied transcript, docs/control/evidence/step-30-40/windows-suite.txt, which is not a test and cannot go red — and it itself records that at the final commit (2c4fd3c8) the windows workflow was NOT green (test stage 0 failed, but the PyInstaller build stage failed with ModuleNotFoundError: typer). The nearest repo test, tests/contract/test_ci_installs_what_the_tests_need.py, only asserts that ci.yml and windows.yml install the same extras, not that any Windows run passed.

### E11-M04-T001
No test anywhere runs check_release against a manifest whose scenario states are 'NOT_RUN' (check_release accepts an arbitrary --manifest path, so such a manifest is a reachable gate input even though finalize refuses to write one). The gate's treatment of literal NOT_RUN verdicts is asserted nowhere: a change to _scenario_verdicts special-casing 'NOT_RUN' as acceptable ('has not run yet, benign') would leave test_check_release.py, test_livetest_runner.py and the whole suite green while the criterion 'all-NOT_RUN fails the gate' is false.

### E12-M02-T002
No test observes 'no eval' or 'no dynamic import from a payload'. A 'liberal' fallback in wire.py that ran eval() or __import__() on payload content would leave test_rpc_wire.py green: every pickle stream fed to eval raises SyntaxError, which would be wrapped as the very RpcError the pickle tests expect, and all well-formed inputs still round-trip through the JSON path. The one gate that does ban eval/exec/pickle.loads — scripts/check_forbidden.py's FORBIDDEN_CALLS — is never executed against the shipped tree by any test: tests/unit/test_check_forbidden.py's real-tree scan (test_the_one_audited_ctypes_use_is_the_one_the_allowlist_names) filters findings to only 'process-tampering' and 'ctypes-outside-the-probe', and no planted-snippet test in that file exercises the eval/exec/pickle.loads rules either. So an eval or dynamic-import path added to the payload decoders would fail no named regression test.

### E06-M04-T001
Nothing observes 'a second process reaches the real core': (1) the named test is single-process (threads) against a Doubles fake; (2) the recorded evidence, packages/pz_agent_cli/src/pz_agent_cli/supervisor.py, is exercised by no test with the router — grep shows serve_rpc/SidecarRpc is never called with CoreRouter anywhere in packages/ or tests/ (tests/unit/test_rpc_sidecar_lifecycle.py drives SidecarRpc with an echo handler only), so if the sidecar never served the router at all, this test stays green; (3) tests/contract/test_mcp_subprocess_e2e.py does launch a genuine second process, but it too reaches a Doubles fake hosted by the test process, not the real core served from the sidecar.

### E11-M04-T006
No assertion anywhere observes that build tasks are BLOCKED (or refused as PASS) while no run has produced the executables. The real build tasks (E11-M02-T001..T008) are PASS with evidence paths that are a CI log (docs/control/evidence/step-30-40/windows-suite.txt) and the .spec file itself (packaging/windows/pz-agent.spec) — files that exist whether or not any executable was ever built — so the gate's evidence-exists check cannot see a spec-without-binary, and flipping a build task to PASS with only a spec on disk fails no test in the named file. scripts/check_master_plan.py's only BLOCKED logic (line 323) is a BLOCKERS.md consistency note, untested here. tests/unit/test_check_release.py::test_an_archive_without_the_executables_is_not_a_candidate guards the RC archive, not the plan's task statuses, so it does not observe this criterion either.

### E12-M02-T003
The criterion is 'every recv has a maximum length', and the actual recv bound lives in packages/pz_agent_core/src/pz_agent_core/rpc/transport.py lines 231 and 364 (connection.recv_bytes(maxlength=MAX_REQUEST_BYTES / MAX_RESPONSE_BYTES)). No test asserts that bound: deleting the maxlength= arguments leaves test_rpc_wire.py (which decodes in-memory bytes) and test_rpc_transport.py (grep finds no maxlength/TooLarge/oversized assertion; it only forbids the pickling Connection.recv) entirely green, while an unbounded peer could then stream arbitrarily many bytes into memory before decode's cap refuses them — exactly the allocation the recv bound exists to prevent.
